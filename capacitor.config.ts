import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'drive_app_user',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
